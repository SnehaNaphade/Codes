// c program to show ASCII values of alphabets

#include<stdio.h>
#include<stdlib.h>

int main()
{
    char ch;
    printf("Enter the alphabet: ");
    scanf("%c",&ch);
    printf("ASCII value of %c is %d",ch,ch);

}